# sync manifest — tracked pushes live here, never commit it
.agent-sync.json

# plugin dependencies (plugins/package.json is committed, modules are not)
node_modules/

# agent friction log (machine-local, never tracked)
.papercuts.jsonl
