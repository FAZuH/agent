name: Release (changelog + commit)

on:
  workflow_call:
    outputs:
      tag:
        description: 'The release tag that was created'
        value: ${{ jobs.publish.outputs.tag }}
    secrets:
      PRIVATE_TOKEN:
        required: true
      DISCORD_RELEASE_WEBHOOK_URL:
        required: false

jobs:
  changelog:
    name: Generate Changelog
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.changelog.outputs.tag }}
      skipped: ${{ steps.changelog.outputs.skipped }}
      changelog: ${{ steps.changelog.outputs.changelog }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Fetch All Tags
        run: git fetch --tags --force origin '+refs/tags/*:refs/tags/*'

      - name: Set Up version.json
        run: |
          LATEST_TAG=$(git tag -l 'v*' --sort=-version:refname | head -1)
          echo "{\"version\":\"${LATEST_TAG:-v0.0.0}\"}" > version.json

      - name: Install changelog dependencies
        run: |
          npm install --no-save --no-package-lock conventional-changelog-conventionalcommits@7.0.2

      - name: Generate Changelog (dry run)
        id: changelog
        uses: TriPSs/conventional-changelog-action@v6
        with:
          skip-commit: true
          skip-tag: true
          github-token: ${{ secrets.PRIVATE_TOKEN }}
          config-file-path: ./.github/.config.cjs
          version-file: ./version.json
          preset: conventionalcommits
          skip-version-file: true
          skip-git-pull: true
          skip-on-empty: false

  publish:
    name: Release Changelog Commit
    needs: changelog
    if: ${{ needs.changelog.outputs.skipped != 'true' }}
    outputs:
      tag: ${{ steps.commit.outputs.tag }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Fetch All Tags
        run: git fetch --tags --force origin '+refs/tags/*:refs/tags/*'

      - name: Set Up version.json
        run: |
          LATEST_TAG=$(git tag -l 'v*' --sort=-version:refname | head -1)
          echo "{\"version\":\"${LATEST_TAG:-v0.0.0}\"}" > version.json

      - name: Install changelog dependencies
        run: |
          npm install --no-save --no-package-lock conventional-changelog-conventionalcommits@7.0.2

      - name: Commit, Tag
        id: commit
        uses: TriPSs/conventional-changelog-action@v6
        with:
          github-token: ${{ secrets.PRIVATE_TOKEN }}
          git-user-name: "faz-bot"
          git-user-email: "mail@fazuh.com"
          config-file-path: ./.github/.config.cjs
          version-file: ./version.json
          preset: conventionalcommits
          skip-version-file: true
          skip-git-pull: true
          skip-on-empty: false
          git-push: false

      - name: Clean stale artifacts and push release
        run: |
          git config user.name "faz-bot"
          git config user.email "mail@fazuh.com"
          git remote set-url origin "https://x-access-token:${{ secrets.PRIVATE_TOKEN }}@github.com/${{ github.repository }}"
          TAG="${{ steps.commit.outputs.tag }}"
          git rm --cached -rf --ignore-unmatch node_modules/ version.json
          git commit --amend --no-edit
          if [ -n "$TAG" ]; then
            git tag -f "$TAG"
          fi
          git push origin release --tags

      - name: Sync release to main
        run: |
          git config user.name "faz-bot"
          git config user.email "mail@fazuh.com"
          git remote set-url origin "https://x-access-token:${{ secrets.PRIVATE_TOKEN }}@github.com/${{ github.repository }}"
          git fetch origin main
          git checkout -B main origin/main
          git merge release --no-edit -m "chore(release): sync release to main [skip ci]"
          git push origin main

  github-release:
    name: Create GitHub Release
    needs: [changelog, publish]
    if: >
      needs.changelog.outputs.skipped != 'true' &&
      needs.changelog.result == 'success' &&
      needs.publish.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Create Release
        id: release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.publish.outputs.tag }}
          body: ${{ needs.changelog.outputs.changelog }}

      - name: Post Release on Discord
        uses: tsickert/discord-webhook@v6.0.0
        with:
          webhook-url: ${{ secrets.DISCORD_RELEASE_WEBHOOK_URL }}
          embed-color: "9498256"
          embed-title: "${{ github.event.repository.name }} ${{ needs.publish.outputs.tag }}"
          embed-description: ${{ needs.changelog.outputs.changelog }}
          embed-url: ${{ steps.release.outputs.url }}