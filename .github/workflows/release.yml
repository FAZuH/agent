name: Release

on:
  push:
    branches:
      - release

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release:
    name: Release (changelog + commit)
    uses: ./.github/workflows/_release.yml
    secrets: inherit